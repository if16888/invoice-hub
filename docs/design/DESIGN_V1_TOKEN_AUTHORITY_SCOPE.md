# Scope

This migration establishes a single runtime authority for shared product tokens.
It does not attempt a blind repository-wide replacement of every historical color
literal. Remaining literals are migrated only when their owning component is
reviewed, so semantic status colors and contrast are not accidentally changed.
