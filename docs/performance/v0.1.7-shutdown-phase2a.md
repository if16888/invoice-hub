# Invoice Hub v0.1.7 Performance Phase 2A

## Scope and provenance

This report records the native Windows shutdown-latency investigation and the
minimal shutdown-path fix on `perf/v017-shutdown-latency`.

- Frozen comparison branch: `perf/v017-performance-baseline`
- Frozen baseline commit: `b28884c39de51a45cb29c7e008ad8d690f1abe3b`
- Optimization branch base: the frozen baseline commit above
- Runtime: Windows 10 build 26200, Python 3.11.9, PyInstaller 6.20.0
- Exercise mode: visible Qt window, normal desktop executable launch, local
  SQLite runtime, real local PDF files, and a desktop-created active mobile
  upload service for the active-server case

No real invoice content, credentials, or private payloads were copied into
the test assets or this report.

## 1. Exact root cause and wait owner

`PERF-SHUTDOWN-001` was confirmed as a Windows Firewall status-query wait,
not a SQLite close wait and not an HTTP-server shutdown wait on the idle path.

The blocking chain was:

1. `InvoiceReviewApp.closeEvent()` synchronously called the mobile controller
   shutdown path.
2. `MobileUploadSessionController.stop()` refreshed formal and development
   firewall status even while the application was closing.
3. The resulting `stopped` signal caused the mobile panel to enter its idle
   state, which refreshed firewall status a second time.
4. Each firewall query launches the hidden PowerShell query with an 8-second
   subprocess timeout. On the observed Windows machine, the actual query
   commonly took about 5--6 seconds.

This explains the stable 11--12 second fresh-idle close time: two sequential
firewall queries were on the GUI close path. The measured SQLite API call was
sub-millisecond, so the old GUI stall label around `db_close` was an upstream
stage-labeling error rather than database evidence.

The unique idle wait owner is therefore the duplicate firewall diagnostics
refresh during synchronous application shutdown.

## 2. Before baseline

The frozen Phase 1.5 native baseline remains unchanged:

| Scenario | Runs | Median | P95 | Max |
| --- | ---: | ---: | ---: | ---: |
| Fresh idle `close_to_hidden` | 10 | 11,739 ms | 12,637 ms | 12,637 ms |
| PDF-loaded trace, run 1 total | 1 | 10,804 ms | -- | -- |
| PDF-loaded trace, run 2 total | 1 | 11,429 ms | -- | -- |

The PDF traces attributed approximately 10.7--11.4 seconds to
`mobile_shutdown`; the actual SQLite close was not the source of that delay.

## 3. Instrumentation and implementation

The opt-in performance probe now has a scoped `performance_stage()` helper.
When performance mode is disabled it performs no clock read and emits no
stage event. When enabled it records begin/end, elapsed milliseconds, thread
identity, active state, and timeout metadata.

Shutdown instrumentation covers:

- mobile controller/session shutdown and start-thread wait;
- HTTP server `shutdown()`, `server_close()`, and conditional server-thread
  join;
- mobile status timer stop;
- mailbox cancellation/wait and migration wait;
- settings flush;
- the actual `sqlite_close_call()` API invocation;
- close-event acceptance and window-hide scheduling.

The behavioral fix is intentionally narrow:

- a shutdown with no starting thread, server, or session takes the inactive
  fast path and does not query the firewall;
- the active shutdown path stops the server without refreshing firewall
  diagnostics;
- the stopped/expired panel state suppresses a diagnostic refresh while the
  application is closing;
- server shutdown joins only an actually live server thread, with a bounded
  two-second protection timeout;
- no product path uses `terminate()`, `kill()`, or `os._exit()` to force
  shutdown.

The startup and ordinary user-triggered firewall status refresh remains
unchanged; it is outside this shutdown fix and is reported as a remaining
diagnostic cost below.

### Follow-up: PERF-SHUTDOWN-002

`migration_worker.wait()` and the mailbox scan worker's `wait()` remain
unbounded waits in the pre-existing shutdown path. They were intentionally not
changed in Phase 2A. `PERF-SHUTDOWN-002` remains `OPEN / NOT REPRODUCED` and
requires a separate boundedness investigation with native evidence before any
implementation change.

## 4. Native after measurements

### Fresh idle / mobile never initialized

Visible native harness, 20 runs:

| Metric | Value |
| --- | ---: |
| `close_to_hidden` min | 883 ms |
| median | 929 ms |
| P95 | 989 ms |
| max | 1,002 ms |
| `hidden_to_exit` | 0 ms on all runs |

The product trace inside one representative run measured about 61 ms through
the close stages; the remaining value is the visible-harness polling and
native window/process observation cost. The hard normal-exit limit of 3,000 ms
passes. The preferred 500 ms close-to-hidden observation target is not met by
the polling harness, while the process-exit P95 is within the 1,000 ms target.

### PDF loaded

Real local one-page PDF, 20 visible row-selection/close runs:

| Metric | Value |
| --- | ---: |
| `close_to_hidden` min | 849 ms |
| median | 864 ms |
| P95 | 905 ms |
| max | 926 ms |
| `hidden_to_exit` | 0 ms on all runs |

No multi-page or greater-than-1 MB PDF was available in the isolated runtime,
so those variants are not claimed as reproduced in this phase.

### Active mobile service

Desktop-created active server, visible native launch, 10 valid runs. One
earlier attempt that showed the panel but did not produce a server-start trace
was excluded as invalid setup evidence.

| Metric | Value |
| --- | ---: |
| `close_to_hidden` min | 811 ms |
| median | 834 ms |
| P95 | 942 ms |
| max | 942 ms |
| `hidden_to_exit` | 0 ms on all runs |

The product stages showed:

- HTTP `shutdown()` / server stop: approximately 307--490 ms;
- session shutdown: approximately 324--511 ms;
- controller shutdown: approximately 325--511 ms;
- settings flush: approximately 10--32 ms;
- actual SQLite close: less than 1 ms.

The active-service total exit P95 is below the 2,000 ms target and the hard
3,000 ms limit.

## 5. Lifecycle matrix

| Case | Native/result evidence | Verdict |
| --- | --- | --- |
| A. App launched, mobile never opened | Fresh-idle 20-run set; no mobile server stage | PASS |
| B. Mobile page opened, service not started | Inactive controller path and regression coverage; no server wait | PASS |
| C. Service started then stopped normally | Bounded server stop and idempotent-stop regression coverage | PASS |
| D. Mobile service currently running | Valid active-service native set, n=10 | PASS |
| E. One mobile upload completed | Physical Android upload was unavailable in this run | BLOCKED |
| F. PDF loaded only | Real local PDF native set, n=20 | PASS |

Case E is deliberately not inferred from the active desktop-created service.
The native upload-completed scenario requires a real device and remains an
acceptance gate for a later run.

## 6. Database and data-safety evidence

After the observational runs:

- SQLite `PRAGMA integrity_check` returned `ok`;
- the runtime database remained readable with 17 invoice rows;
- observed review status counts remained `approved=4` and `to_review=13`;
- settings remained readable after restart;
- no upload/import mutation was performed by the shutdown benchmark;
- no residual InvoiceHub or benchmark wrapper process remained;
- all recorded native runs reached process exit with `hidden_to_exit=0`;
- repeated visible start/close runs did not leave an orphan server or worker.

The fix does not force-kill a worker or process and does not alter SQLite
pragmas or transaction semantics.

## 7. Remaining stages over 100 ms

- Active HTTP server shutdown is the only normal shutdown stage observed in
  the hundreds-of-milliseconds range: approximately 307--490 ms.
- Settings flush was approximately 10--32 ms in the observed runs.
- Actual SQLite close was below 1 ms.
- The 5--6 second firewall query remains a startup/ordinary diagnostics cost,
  but no longer runs unconditionally during application close.

There is no evidence that database close, WAL checkpointing, or a database
lock owns the former 10--12 second idle wait.

## 8. User-symptom correlation

| User symptom | Native evidence | Conclusion |
| --- | --- | --- |
| Window stays visible for 11--12 seconds on idle close | Two sequential firewall status queries on the close path | Reproduced and fixed |
| PDF-loaded close feels equally slow | `sqlite_close_call <1 ms`; mobile/firewall path dominated before fix | Not a PDF/SQLite close root cause |
| Active mobile service takes longer than idle | HTTP server shutdown around 307--490 ms | Expected bounded active-service cleanup, within target |

## 9. Validation

Already completed before final full-suite collection:

- targeted shutdown/mobile/performance tests: `65 passed`;
- HCI acceptance: `19 / 19 PASS`;
- HCI oracle: `17 passed`;
- isolated unit shard 0: `531 tests, 0 skipped`;
- isolated unit shard 1: `402 tests, 0 skipped`;
- isolated unit shard 2: `430 tests, 5 skipped`;
- compileall, repository privacy gate, public-export gate, and diff-check:
  PASS.

Final full pytest: `1449 passed, 6 skipped, 125 subtests passed` in
1,041.67 seconds.

## 10. Phase 2A verdict and next priorities

```text
SHUTDOWN ROOT CAUSE = CONFIRMED
SHUTDOWN DATA SAFETY = PASS
SHUTDOWN NATIVE LATENCY = PASS (hard normal-exit targets)
PERFORMANCE NATIVE OBSERVABILITY = PASS
USER SYMPTOM REPRODUCTION = PASS for shutdown; mobile upload completion BLOCKED
PHASE 2 OPTIMIZATION TARGETS = EVIDENCE-BASED
```

Recommended follow-up order, only with new native before/after evidence:

1. Keep the shutdown fast path and add real-device upload-completed lifecycle
   evidence.
2. If active-service shutdown regresses above the target, isolate the HTTP
   server wake-up/poll interval as a separate change.
3. Treat startup/ordinary firewall diagnostics as a separate UX/performance
   topic; do not reintroduce it into the close path.
4. Do not optimize SQLite close or alter database pragmas without new timing
   evidence.
