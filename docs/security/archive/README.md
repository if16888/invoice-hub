# v0.1.8 历史代码审查归档

本目录保存两份在 v0.1.8 发布准备期间产生的只读代码审查报告：

- `v0.1.8-code-review-20260830.md`：第一轮审查，原始日期 2026-08-30。
- `v0.1.8-code-review-20260903.md`：第二轮审查，原始日期 2026-09-03。

## 证据边界

两份报告都是 **pre-remediation historical evidence**。报告正文和当时的风险判断保留用于追溯，但其中的行号、测试数量、`NO-GO`/`CONDITIONAL REJECT`、P0/P1 分级和“必须发布前修复”的判断，只适用于各自的历史审查基线，不能直接代表当前 master 或后续候选版本。

本归档创建时核对的代码基线为：

```text
master = c54b2267a845e8eb3648dc24dafd0a04550425c1
```

相对于两份历史报告，当前代码已确认发生过的变化包括：

- master CI 已包含 `push` 触发；
- 发票去重已要求稳定发票身份，无发票号时不再仅凭销售方和金额判重；
- 历史报告中提到的外来电力测试文件和目录当前不存在；
- 重下载/邮箱连接的异步边界、退出生命周期、Excel 安全转义和数据库回滚已有后续修复；
- `QApplication` 单例问题和 `preview_mixin.py` 顶部重复定义已不再对应当前代码。

同时，以下内容在归档时仍应作为独立后续审查项处理：

- 设置页 AI 测试按钮仍需确认是否会给出未经实际请求验证的成功提示；
- `services._rename_source_mode` 的模块级状态和跨模块写入仍是架构技术债；
- Native GUI geometry 继续是已知 non-gating lane；
- 报告中关于并发、IMAP 故障注入、搜索性能和测试 mock 边界的建议，需要基于当前代码单独复核，不能仅凭历史报告判定已修复或仍为 blocker。

本索引和两份报告不改变产品代码、依赖、工作流、release tag 或 release asset。
